<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "kr.co.bitnews.mapper.UserMapper">

	<resultMap id="usermap" type="user">
		<result column="user_id" property="userId"/>
		<result column="user_pw" property="userPw"/>
		<result column="user_name" property="userName"/>
		<result column="user_email" property="userEmail"/>
	</resultMap>
	

	<select id = "selectUserByID" resultType="user" parameterType="string">
		select *
		  from tb_user
		 where user_id = #{userId} and 
		 	   user_pw = #{userPw}
	</select>
	
	<select id = "selectID" resultType="string" parameterType="user">
		select user_id
		  from tb_user
		 where user_name = #{userName} and
		 	   user_email = #{userEmail}
	</select>
	
	<select id = "selectPW" resultType="string" parameterType="user">
		select user_pw
		  from tb_user
		 where user_name = #{userName} and
		 	   user_email = #{userEmail} and
		 	   user_id = #{userId}
	</select>
	
	<insert id="insertUser" parameterType="user">
		insert into tb_user(
			user_id,
			user_pw,
			user_name,
			user_email
		)values (
			#{userId},
			#{userPw,
			#{userName},
			#{userEmail}
		)
	</insert>
	
	<update id="updateUser" parameterType="user">
		update tb_user
		   set user_pw = #{userPw}
		   	   user_name = #{userName}
		   	   user_email = #{userEmail}
		 where user_id = #{userId}
	</update>
	
	<update id="withdrawUser" parameterType="string">
<<<<<<< HEAD
		update tb_user
		   set user
=======
	update tb_user
	   set user_withdraw = 1
	 where user_id = #{id}
>>>>>>> 40b2d2b8f4f28271654386707df7e580ae9ad35b
	</update>
	
	
</mapper>
